name: testing-yc-network
runtime: yaml

variables:
  prefix: ${config:prefix}

resources:
  network:
    type: yandex:VpcNetwork
    properties:
      name: ${prefix}-testing-yc-network

outputs:
  networkId: ${network.id}
