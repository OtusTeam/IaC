set -x
echo "secret_username: {$ansible-vault encrypt_string very_secure_password}" > 
ansible-vault encrypt string admin > encrypted_login
echo "secret_username: {$ansible-vault encrypt_string very_secure_password}" > 


