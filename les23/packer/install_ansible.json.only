{
  "builders": [
    {
      "communicator": "ssh",
      "source_path": "/media/za/1/images/jammy-server-cloudimg-amd64-vagrant.box",
      "provider": "virtualbox",
      "add_force": true,
      "type": "vagrant"
    }
  ],
  "provisioners": [
    {
      "inline": [
        "sudo apt-get install -y -qq pip3",
        "sleep 10",
        "python3 -m pip install ansible"
      ],
      "type": "shell"
    }
  ]
}

